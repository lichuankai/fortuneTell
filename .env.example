# 本地开发 / vite preview：仅由 Vite 读取，用于 dev server 代理往 DeepSeek 注入 Authorization（勿提交 .env）
DEEPSEEK_API_KEY=

# 生产静态资源不会读取本文件里的 DEEPSEEK_API_KEY。线上必须在「能处理 /api/deepseek 的那一层」配密钥：
# - 方案 A：部署 server/deepseek-proxy.mjs，在运行该 Node 进程的环境中 export DEEPSEEK_API_KEY，并设防火墙仅允许你的前端域名访问；
# - 方案 B：Nginx/Caddy 等同源反代 /api/deepseek -> https://api.deepseek.com/v1/... 并加 proxy_set_header Authorization "Bearer ..."；
# 构建时若接口与静态页不同源，再设置下一行，然后 npm run build
# VITE_API_PROXY_URL=https://你的代理服务根地址（不要尾斜杠）
